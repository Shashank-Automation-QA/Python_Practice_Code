#Unpack tuple = (1,2,3) into variables like a=1, b=2 and c=3

#Count total elements in nested tuple = (1,(2,5),(4,3))  ==>>  output 5

#Flatten a nested tuple = (1,(2,5),(4,3))  ==>>  (1,2,3,4,5)

#Write program to return multiple values using tuple.  (check tuple practice file for solution)

# Sort a list without using sort()

#Rotate a list